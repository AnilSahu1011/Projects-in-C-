const { sum } = require("lodash")

console.log('This is my first script')
const obj = {
    height: 5,
    width:10,
    draw: () => this.height*this.width //arow function
}
function add(a, b){
    return sum = {
        sum2(){
            console('a+b')
        }
    }
}
console.log(add.sum.sum2)